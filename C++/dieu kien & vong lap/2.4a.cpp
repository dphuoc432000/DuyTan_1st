#include <iostream>
#include <cmath>
#include <cstring>
#include <fstream>
using namespace std;
main()
{
	float a, b, A, B, C, x, y, d;
	cin >> (a, b) >> (A * x + B * y + C = 0);
	d = (abs(A * a + b * B + C)) / (sqrt(pow(A, 2) + pow(B, 2)));
	cout << "Khoang cach la: " << d;
}
